import {useState} from 'react';
import {useVillage} from './store';
import {DECORATIONS,REQUESTS,decorationUnlocked,requestMissing,LABEL,JOBS,JOB_NAMES,type Decoration,type RequestId,type Resource,validName} from './model';
export function DecorPanel({onPlace}:{onPlace:(d:Decoration)=>void}){const v=useVillage(s=>s.v);return <><h2>แต่งให้เป็นบ้านของเรา</h2><p>วางร่วมกับอาคารได้ พื้นที่ละ 1 ชิ้น เก็บคืนวัสดุเต็มจำนวน ไม่เพิ่มแต้ม ไม่ต้องรีบ</p>{(Object.keys(DECORATIONS) as Decoration[]).map(d=><button className="blueprint" key={d} disabled={!decorationUnlocked(v,d)} onClick={()=>onPlace(d)}><span className="decoration-glyph">{DECORATIONS[d].symbol}</span><span><strong>{DECORATIONS[d].name}</strong><small>{decorationUnlocked(v,d)?Object.entries(DECORATIONS[d].cost).map(([r,n])=>`${LABEL[r as Resource]} ${n}`).join(' · '):d==='bench'?'เปิดจากคำขอสวนหน้าบ้าน':'เปิดจากคำขอแสงอุ่นจากเตาเผา'}</small></span></button>)}</>}
export function RequestsPanel(){const {v,dispatch}=useVillage();return <><h2>จดหมายจากเพื่อนบ้าน</h2><p>ทำเมื่อพร้อม ไม่มีเวลาหมด รางวัลรับได้คำขอละครั้ง</p>{(Object.keys(REQUESTS) as RequestId[]).map(id=>{const r=REQUESTS[id],done=v.completed.includes(id),missing=requestMissing(v,id);return <article className="cozy-request" key={id}><small>จาก {v.names[r.from]}</small><h3>{r.title}</h3><p>“{r.text}”</p><small>ของขอบคุณ: {r.reward}</small>{!done&&missing&&<p>{missing}</p>}<button className="village-primary" disabled={done||!!missing} onClick={()=>dispatch({type:'fulfill',request:id})}>{done?'✓ ส่งมอบแล้ว':id==='garden'?'พาไปชมสวน':'ส่งมอบของตามคำขอ'}</button></article>})}</>}
function PersonName({person}:{person:number}){const {v,dispatch}=useVillage(),[edit,setEdit]=useState(false),[name,setName]=useState('');return <div className="person-name"><span className="person-avatar">{['✿','♧','❋','☀','♡'][person]}</span>{edit?<form onSubmit={e=>{e.preventDefault();if(validName(name,20)){dispatch({type:'rename-person',person,name});setEdit(false);}}}><input aria-label={`ชื่อชาวบ้านคนที่ ${person+1}`} maxLength={40} value={name} onChange={e=>setName(e.target.value)} autoFocus/><button disabled={!validName(name,20)}>บันทึก</button><button type="button" onClick={()=>setEdit(false)}>ยกเลิก</button></form>:<><span>{v.names[person]}<small>สมาชิกคนที่ {person+1}</small></span><button onClick={()=>{setName(v.names[person]);setEdit(true);}}>ตั้งชื่อ</button></>}</div>}
export function Neighbors(){const v=useVillage(s=>s.v);return <details className="neighbors"><summary>รู้จักเพื่อนบ้านของเรา · {v.people} คน</summary>{Array.from({length:v.people},(_,i)=><PersonName key={i} person={i}/>)}<small>เลือกชาวบ้านด้านล่างเพื่อมอบหมายงานทีละคน</small></details>}
export function VillageName(){const {v,dispatch}=useVillage(),[name,setName]=useState(v.name);return <form className="village-name-form" onSubmit={e=>{e.preventDefault();dispatch({type:'rename',name});}}><label>ชื่อหมู่บ้าน<input value={name} maxLength={64} onChange={e=>setName(e.target.value)}/></label><button disabled={!validName(name,32)}>บันทึกชื่อหมู่บ้าน</button><small>ตั้งชื่อได้ 1–32 ตัวอักษร</small></form>}
